apiVersion: apps/v1
kind: Deployment
metadata:
  name: cosy-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cosy-backend
  template:
    metadata:
      labels:
        app: cosy-backend
    spec:
      containers:
        - name: cosy-backend
          image: ghcr.io/magenta-mause/cosy-backend:sha-449db7d
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: cosy-postgresql-credentials
                  key: postgresql-username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cosy-postgresql-credentials
                  key: postgresql-password
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://cosy-postgres:5432/cosydb
            - name: COSY_LOKI_USER
              valueFrom:
                secretKeyRef:
                  name: cosy-loki-credentials
                  key: user
            - name: COSY_LOKI_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cosy-loki-credentials
                  key: password
            - name: COSY_LOKI_URL
              value: http://cosy-loki-nginx:80
            - name: COSY_INFLUX_URL
              value: http://cosy-influxdb:8086
            - name: COSY_INFLUX_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cosy-influxdb-secrets
                  key: DOCKER_INFLUXDB_INIT_ADMIN_TOKEN
          volumeMounts:
            - name: docker-socket
              mountPath: /var/run/docker.sock

          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
            requests:
              memory: "256Mi"
              cpu: "250m"

          livenessProbe:
            httpGet:
              path: /api/actuator/health
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/actuator/health
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 5

      volumes:
        - name: docker-socket
          hostPath:
            path: /var/run/docker-non.sock
